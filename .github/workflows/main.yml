name: main

on: [push]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@master
      - name: Run build script
        working-directory: ./tools
        run: bash ./build.bash
