name: happy-day-ci

on:
  push:
    paths:
      - 'services/happy-day/**'

defaults:
  run:
    working-directory: ./services/hello-world

jobs:
  build:
    runs-on: ubuntu-20.04
    steps:
      - uses: actions/checkout@master
      - run: bash ./build.bash
  deploy-to-production:
    if: ${{ github.ref }} == "refs/headers/master"
    needs: [build, test]
    runs-on: ubuntu-20.04
    steps:
      - run: echo "hello world!"
  deploy-to-staging:
    if: ${{ github.ref }} == "refs/headers/development"
    needs: [build, test]
    runs-on: ubuntu-20.04
    steps:
      - run: echo "hello world!"
  test:
    needs: [build]
    runs-on: ubuntu-20.04
    steps:
      - run: echo "hello world!"
